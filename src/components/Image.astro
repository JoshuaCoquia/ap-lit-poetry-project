---
interface Props {
	src: string;
    alt?: string;
    citationLink: string;
    citationText: string;
    width?: string;
    height?: string;
}

const { src, alt, citationLink: href, citationText, width, height } = Astro.props;
import Citation from './Citation.astro';
---
<div>
    <img src={src} alt={alt} width={width} height={height}>
    <br>
    <Citation href={href} citationText={citationText} />
</div>

<style>
    div {
        margin: 3rem 0rem;
        text-align: center;
        overflow: hidden;
    }
</style>